<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.waters.sysdev.gss_timer_2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity_ModuleSelect"
            android:label="@string/title_activity__module_select"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_StopWatch"
            android:label="@string/title_activity__stop_watch"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_CompletedTasks"
            android:label="@string/title_activity__completed_tasks"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>